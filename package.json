{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm install && nuxt build",
    "dev": "npm install && npm i usebootstrap && nuxt dev",
    "generate": "npm install && npm i usebootstrap && nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@types/lodash-es": "^4.17.12",
    "nuxt": "^3.8.2",
    "vue": "^3.3.8",
    "vue-router": "^4.2.5"
  },
  "volta": {
    "node": "18.16.1"
  },
  "dependencies": {
    "lodash-es": "^4.17.21",
    "usebootstrap": "^0.1.88"
  }
}
